# Environment & secrets
.env
.env.*
!.env.example
config/users/qa.users.ts
config/users/dev.users.ts
config/users/ppr.users.ts

# Dependencies
node_modules/

# Test artifacts
test-results/
allure-results/
allure-report/
playwright-report/
blob-report/
traces/
storage/

# Build output
dist/

# Persisted state (generated at runtime)
.doc-state.json
.product-state.json
.auth-state.json
.auth/

# Temporary debug / diagnostic scripts
diagnose-*.mjs
diagnose-*.ts
tmp/

# Tracker DB (SQLite — rebuild via `npm run tracker:sync`)
config/scenarios.db
config/scenarios.db-wal
config/scenarios.db-shm

# IDE & OS
**/.DS_Store
.vscode/

# Lock file (optional — uncomment to track)
# package-lock.json
